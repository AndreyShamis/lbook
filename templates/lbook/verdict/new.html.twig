{% extends 'lbook/default/index.html.twig' %}

{% block content %}
    <h1>Verdict creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('verdict_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
