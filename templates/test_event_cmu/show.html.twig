{% extends 'lbook/default/index.html.twig' %}
{% block title %}EventMCU Info{% endblock %}
{% block content %}
    <h1>TestEventCmu</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ test_event_cmu.id }}</td>
            </tr>
            <tr>
                <th>Block</th>
                <td>{{ test_event_cmu.block }}</td>
            </tr>
            <tr>
                <th>Fault</th>
                <td>{{ test_event_cmu.fault }}</td>
            </tr>
            <tr>
                <th>A_value</th>
                <td>{{ test_event_cmu.aValue }}</td>
            </tr>
            <tr>
                <th>B_value</th>
                <td>{{ test_event_cmu.bValue }}</td>
            </tr>
            <tr>
                <th>A_time</th>
                <td>{{ test_event_cmu.aTime }}</td>
            </tr>
            <tr>
                <th>B_time</th>
                <td>{{ test_event_cmu.bTime }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ test_event_cmu.createdAt ? test_event_cmu.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('test_event_cmu_index') }}">back to list</a>

    <a href="{{ path('test_event_cmu_edit', {'id': test_event_cmu.id}) }}">edit</a>

    {{ include('test_event_cmu/_delete_form.html.twig') }}
{% endblock %}
