{% extends 'lbook/default/index.html.twig' %}
{% block title %}Test Filter Apply History{% endblock %}
{% block content %}
    <h1>TestFilterApply</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ test_filter_apply.id }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ test_filter_apply.createdAt ? test_filter_apply.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('test_filter_apply_index') }}">back to list</a>

    <a href="{{ path('test_filter_apply_edit', {'id': test_filter_apply.id}) }}">edit</a>

    {{ include('test_filter_apply/_delete_form.html.twig') }}
{% endblock %}
