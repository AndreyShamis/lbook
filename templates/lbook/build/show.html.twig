{% extends 'lbook/default/index.html.twig' %}

{% block title %}LogBookBuild{% endblock %}

{% block content %}
    <h1>LogBookBuild</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ log_book_build.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ log_book_build.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('log_book_build_index') }}">back to list</a>

    <a href="{{ path('log_book_build_edit', {'id': log_book_build.id}) }}">edit</a>

    {{ include('lbook/build/_delete_form.html.twig') }}
{% endblock %}